<%= form_with(model: bolao, local: true) do |form| %>
  <% if bolao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bolao.errors.count, "error") %> prohibited this bolao from being saved:</h2>

      <ul>
      <% bolao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :ativo %>
    <%= form.check_box :ativo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
